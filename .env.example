# ============================================
# 环境变量模板
# 使用说明：
# 1. 复制此文件为 .env
# 2. 填入真实值（API Key、密码等）
# 3. .env 已在 .gitignore 中，不会提交到仓库
# ============================================

# ---------- LLM 提供商 ----------
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
# 注意变量名必须是 DEEPSEEK_API_BASE，不能写成 DEEPSEEK_BASE_URL：
# langchain-deepseek 的 chat_models.py 中 api_base 字段绑定的是 from_env("DEEPSEEK_API_BASE")，
# 写错名字不会报错，只会被静默忽略并回退到官方默认端点。
DEEPSEEK_API_BASE=https://api.deepseek.com/v1  # 可选，默认即官方端点
DEEPSEEK_MODEL=deepseek-v4-pro

# ---------- PostgreSQL 数据库 ----------
# 本地开发（直连宿主机）
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=contract_rag

# 容器内连接（使用 Docker 服务名）
# 注释掉上面 localhost 行，取消下面注释
# POSTGRES_HOST=postgres

# 完整连接字符串（由程序拼接，也可直接指定）
# DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ---------- Redis ----------
# 本地开发
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# 容器内连接
# REDIS_HOST=redis

# Celery Broker URL（由程序拼接）
# CELERY_BROKER_URL=redis://${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}
# CELERY_RESULT_BACKEND=redis://${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

# ---------- Qdrant 向量数据库 ----------
# 本地开发
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334  # 本项目中未使用 gRPC，保留备用

# 容器内连接
# QDRANT_HOST=qdrant

# Qdrant 集合名称
QDRANT_COLLECTION_NAME=contract_chunks

# ---------- LangFuse 可观测性 ----------
# 注册地址：https://cloud.langfuse.com
# 注册后在项目 Settings → API Keys 中获取以下密钥
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxx
LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxx

# ---------- Embedding 模型 ----------
# 使用 sentence-transformers 本地模型
EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5  # 中文小模型，平衡速度与效果

# 跨编码器（重排序用）
CROSS_ENCODER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# ---------- 应用配置 ----------
APP_NAME=Contract RAG Agent
APP_DEBUG=false
APP_LOG_LEVEL=INFO

# API 前缀
API_V1_PREFIX=/api/v1

# 文件上传限制（字节）
MAX_UPLOAD_SIZE=10485760  # 10MB

# 临时文件目录
UPLOAD_DIR=./uploads

# ---------- 检索参数 ----------
# 召回 top-k（多路召回后融合）
RETRIEVAL_TOP_K=20

# 重排序后最终返回数量
RERANK_TOP_K=5

# RRF 融合参数 k
RRF_K=60

# ---------- 分块参数 ----------
# 条款分块最小字符数（低于此会合并）
MIN_CHUNK_SIZE=100

# 最大字符数（超过会强制截断）
MAX_CHUNK_SIZE=1500

# ---------- Celery 配置 ----------
CELERY_TASK_TIME_LIMIT=300  # 任务超时时间（秒）
CELERY_TASK_SOFT_TIME_LIMIT=240
CELERY_WORKER_CONCURRENCY=4